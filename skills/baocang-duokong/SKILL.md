---
name: baocang-duokong
description: |-
  合约爆仓多空观测的命令行战报技能（Liquidation & Long/Short Observatory）。
  面向完全没写过代码的使用者：报告某个 USDT 本位永续合约（如 BTCUSDT）近 24 小时的
  多头/空头爆仓金额分布、单笔大额爆仓、未平仓名义价值与全局多空账户比，并按引擎口径
  给出「平稳 / 交火 / 激战」三档战况等级。判定引擎可复现、不依赖大模型。
  当用户想知道「这个币最近谁在被爆仓」「多空哪边伤亡更重」「现在战况激不激烈」，
  或给出一个合约交易对要求观测爆仓与多空态势时使用。
  不用于：预测价格涨跌、给出开仓建议、或任何交易执行。
metadata:
  version: 1.0.0
  author: DDDDDNBNB11
  category: market-observation
  language: zh-CN
  license: MIT
official_resources:
  - binance-cli（官方 CLI，request GET 直取合约实时接口）
  - data.binance.vision（官方开源数据仓库：合约 K 线 / 合约指标 / 资金费率归档）
  - fapi.binance.com（官方合约公开实时接口，经自动选路）
optional_skills:
  - binance
---

# 战情室值机手册 · 合约爆仓多空观测

## 这个战情室播报什么

一个币的 USDT 本位永续合约，近 24 小时「谁在被爆仓、爆了多少、多空哪边伤亡更重」：

- **伤亡统计**：每小时多头/空头爆仓金额（UTC 分桶）、火力峰值、全天合计 —— 金额为
  「K 线振幅 × 资金费率」折算估算（官方实时强平明细端点已下线，实测 404），战报里印「口径提示」；
- **重大伤亡**：单笔 ≥ 100 万 USDT 的真实逐笔爆仓（最多 8 条）—— 折算口径下本区如实留空；
- **兵力与士气**：未平仓名义价值 + 全局多空账户比（含 24 小时真实序列）；
- **战况等级**：引擎按「峰值小时爆仓 ÷ 全时均值」定档 —— ≤2 倍 平稳 ◆、2~3 倍 交火 ◆◆、>3 倍 激战 ◆◆◆。

强平方向口径：强平单 side=SELL = 多头仓位被强平 = 「多头」伤亡（与网页版引擎一致）。

## 不播报什么

- 不预测价格方向，不给出开平仓建议；
- 不报「未来会不会爆仓」——爆仓数据只反映已发生的强平；
- 不报逐笔委托簿、资金费率套利这些别的作品已经在做的事。

## 四条侦察线路（数据通道）

| 线路 | 命令 | 数据来源 | 适用 |
|---|---|---|---|
| 默认（自动选路） | `python agent.py BTCUSDT` | 官方合约实时接口 fapi.binance.com（先直连、连不上自动换用户本机上网工具的 6 个通道），与网页同口径 | 日常观测 |
| 强制刷新 | `python agent.py BTCUSDT --live` | 同上，但重新选路 | 网络环境刚变化、怀疑线路失灵 |
| 官方 CLI | `python agent.py BTCUSDT --skill` | 官方 CLI `binance-cli request GET` 直取同一组合约端点 | 要体现官方技能取数时 |
| 官方归档 | `python agent.py BTCUSDT --official` | 官方开源数据仓库：1h K 线 + 月度资金费率 → 折算爆仓压力；合约指标 → 真实持仓量与账户比 | 接口不通、或要历史某天（T+1） |

自然语言也认：`python agent.py "用官方归档看看 BTCUSDT 昨天的战况"` 自动选通道；
显式旗标永远优先，识别不出就静默走默认，绝不乱猜币种。

## 战报每个栏目是什么

- **战况等级**：引擎 alert（正常/警告/极端）的战报化表述，倍数依据直接印在报告里；
- **伤亡统计**：引擎 analyze 的 24 小时分桶原样输出（判定逻辑来自引擎，通道只喂真实数据）；
- **重大伤亡**：真实逐笔（单笔 ≥100 万 USDT），**不足不补** —— 引擎内的「代表性补录」已在战报层剔除；
- **兵力与士气**：持仓量与账户比均为真实值；走势序列在实时通道是账户比 24h 序列、在归档通道是持仓量+账户比每小时序列（口径印在报告里）；
- **参谋提示**：本技能的诚实边界，逐条印在每份战报末尾。

## 官方资源声明

本技能用到的官方资源只有上面四条线路列出的三样：官方 CLI、官方开源数据仓库、官方合约公开实时接口。
官方 20 个技能中实际直接调用 1 个（`binance`，经其官方 CLI）；其余 19 个的取舍理由见项目 AGENT.md，
不做挂名。判定引擎是自研规则引擎，可复现、不依赖大模型。

## 别过度解读的地方

1. **爆仓金额是折算估算**：官方实时强平明细端点（`/fapi/v1/allForceOrders`）已下线（实测 404），
   历史数据仓库里也没有强平明细归档 —— 实时类与归档通道的爆仓金额都按
   「K 线振幅 × 资金费率」折算（与网页部署版同一套公式），JSON 里 `estimate=true` 可核验；
   持仓量 / 价格 / 多空账户比 / 资金费率为官方真实值；
2. 账户比是「账户数」之比，不等于资金量之比；
3. 实时强平接口只返回最近约 1000 笔，深夜清淡时段样本天然偏少；
4. 引擎原有的「持仓趋势」示意曲线不进战报（战报只报真实序列）；
5. 网页版在接口不可用时会展示演示数据（原作品设计如此并在页面标注）；命令行版绝不使用演示数据，
   取不到就如实报错退出。
